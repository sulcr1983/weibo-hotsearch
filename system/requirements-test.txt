#
# weibo-hotsearch 测试依赖
# 安装: pip install -r system/requirements-test.txt
#

# ── 基础测试框架 ──
pytest>=7.4
pytest-asyncio>=0.23
pytest-cov>=4.1

# ── HTTP Mock/录制 ──
pytest-vcr>=1.0
vcrpy>=5.1

# ── 性能基准（可选） ──
# pytest-benchmark>=4.0     # 取消注释以启用 benchmark
# objgraph>=3.5             # 对象增长可视化

# ── 断言增强（可选） ──
# pytest-assume>=2.4        # 软断言